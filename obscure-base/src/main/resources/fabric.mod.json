{
    "schemaVersion": 1,
    "id": "${module_id}",
    "version": "${module_version}",
    "name": "${module_name}",
    "authors": [ "Sindercube" ],
    "icon": "assets/obscure/icon.png",
    "environment": "*",
    "custom": { "modmenu": { "parent": "${mod_id}" } }
}
